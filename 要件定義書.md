# Health Web App version2 要件定義書

作成日: 2026-08-19

## 0. 本書の位置づけ

本書は、Health Web App version2（以下「本アプリ」）を**新規に構築するための確定仕様**である。以前存在した実装（不具合により作り直しとなったもの、および `C:\Health-Web-App` に存在する無関係な別プロジェクト）とは切り離された、独立した仕様として扱う。

- 本書は機能・データモデル・API仕様の**唯一の正**である。
- 開発体制・分業ルール・品質ゲート・Git運用は `Health_Web_App_マルチエージェント開発要件定義書.md` を参照する。両者は対になって運用され、本書は「何を作るか」、マルチエージェント要件定義書は「誰が・どう作るか」を定義する。
- 「設計・文書化のみで初期スコープに含めない項目」は、第14章に分離して明示する。

---

## 1. システム概要

iPhone（Safari／ホーム画面追加PWA）での利用を主対象とした、**個人向けの健康・食事・習慣管理Webアプリケーション**。身体測定、運動・ストレッチ、睡眠・水分・体調、サプリメント、食事・レシピ・献立、食品在庫・買い物、カレンダー、通知、レポートを1つのアプリで記録・振り返る。

基本方針は以下のとおりで、全機能がこれに従う。

| 方針 | 内容 |
|---|---|
| プライバシー優先 | 健康データは所有者ごとにRow Level Securityで分離。秘密値はサーバー境界の内側だけで扱う |
| ローカルファースト | 各機能はIndexedDB（Dexie）へ先に保存し、認証時はサーバーAPIと同期する。オフラインでも記録できる |
| ゼロコスト既定 | AI・画像解析は無料枠／ローカル／決定論的フォールバックのみ。有料プロバイダーは既定で拒否する |
| 承認主義 | AI提案・画像解析結果は自動確定せず、利用者が確認・修正して保存した時点で初めて反映する |
| 非医療 | 医療機器ではなく、診断・治療・投薬判断を行わない。推定値は推定と明示する |

- 初期言語: 日本語（`<html lang="ja">`）
- 既定タイムゾーン: `Asia/Tokyo`（プロフィールでIANA名を変更可能）
- HealthKit連携は Web/PWA から利用できないため対象外

---

## 2. 技術構成

| 項目 | 内容 |
|---|---|
| フレームワーク | Next.js 16.2.x（App Router、`--webpack`）、React 19.2.x |
| 言語 | TypeScript 5.9.x（strict）、Zod 4.x による入出力検証 |
| スタイル | Tailwind CSS 4.3.x + CSS Modules（`globals.css` と機能別 `*.module.css`） |
| DB/認証/ストレージ | Supabase（PostgreSQL、Auth、Storage）、`@supabase/ssr` / `@supabase/supabase-js` |
| ローカル保存 | IndexedDB（Dexie / dexie-react-hooks） |
| PWA | 自前 Service Worker `public/sw.js`（`@serwist/next` を依存として導入） |
| Push | Web Push（`web-push`、VAPID） |
| AI | `@google/genai`（Gemini無料枠）、ローカルOllama、決定論的フォールバック |
| 画像処理 | クライアント: Canvas再エンコード、サーバー: `sharp` |
| テスト | Vitest（+ PGlite によるmigration検証）、Playwright、Testing Library |
| 実行環境 | Node.js 26.x / npm 11.x |
| 配置先 | Vercel（Next.js）+ Supabase（DB/Auth/Storage）を前提とした構成 |

具体的なバージョン固定値は `Health_Web_App_マルチエージェント開発要件定義書.md` Phase 0の指示、および実装時の`package.json`を正とする（本書は世代・メジャーバージョンの方針のみを定める）。

### 2.1 ディレクトリ構成

```text
src/
  app/                  # App Router のページとRoute Handler
  components/           # 共通UI（button, card, field, status-banner, navigation, pwa）
  features/             # 機能別のスキーマ・ストア・ワークスペースUI
    auth, profile, account, body-measurements, workouts, wellness,
    supplements, meals, food-image, pantry, calendar, google-calendar,
    notifications, assistant, reports, offline-sync, demo
  lib/supabase/         # ブラウザ／サーバー／プロキシ用クライアントと設定
  server/               # サーバー専用処理（account, assistant, food-image,
                        # google-calendar, notifications）
supabase/migrations/    # SQL migration
supabase/tests/database # pgTAP（スキーマ契約、RLS分離）
public/sw.js            # Service Worker
e2e/ , staging-e2e/     # Playwright
tests/                  # DB/設定/ステージング検証のVitest
scripts/                # 環境検証・リリース／ステージング検査スクリプト
```

`src/tests/server-only.ts` と `import "server-only"` により、サーバー専用モジュールのクライアント混入を禁止する。

---

## 3. 利用者と実行モード

本アプリは単一の個人ユーザー利用を主対象とするが、DBは複数ユーザーを完全分離する。役割による機能差はなく、**実行モードによって保存先が切り替わる**。

### 3.1 デモモード（資格情報不要）

- `/demo` で利用でき、Supabase・外部APIの設定なしに動作する。
- 架空の初期候補プロフィールを提示し、利用者が確認した内容だけをブラウザのIndexedDBへ保存する。
- Supabaseや外部プロバイダーへ一切送信しない。JSON出力とブラウザ内データ削除を画面から実行できる。
- 有効化フラグ: `NEXT_PUBLIC_DEMO_MODE_ENABLED`。

### 3.2 認証済みモード

- Supabase Authのセッションを持つ利用者。各機能はローカルIndexedDBに保存しつつ、owner-scopedなAPIでSupabaseへ同期する。
- 所有者IDは**必ず検証済みサーバーセッションから導出**し、リクエストボディの `owner_id` / `user_id` は拒否する。

### 3.3 未認証・未設定時のふるまい

- Supabase未設定時は、アカウントAPIは HTTP 503（`ACCOUNT_SERVICE_UNAVAILABLE`）を返し、UIはデモモードへの導線を示す。
- 保護ルート（`/records` `/measurements` `/workouts` `/meals` `/pantry` `/calendar` `/reports` `/settings` `/onboarding` `/auth/session` など）は、プロキシ（`src/lib/supabase/proxy.ts` の `protectedRoutePrefixes`）でセッションと利用者状態を確認する。

---

## 4. 画面構成

下部固定ナビゲーション（`MobileNavigation`）は「AI／ホーム／カレンダー／記録／食事／レポート／その他」の7項目。

| ルート | 画面 | 主な役割 |
|---|---|---|
| `/` | ようこそ／ホーム | サービス説明と、認証済み時は今日の状況・クイック操作を表示するダッシュボード（初期スコープに含む。詳細は5.14節） |
| `/demo` | ローカルデモ | 架空プロフィール編集、IndexedDB保存、JSON出力、ブラウザ内削除 |
| `/onboarding` | 初回セットアップ | 候補プロフィールの確認・編集・保存 |
| `/auth` | ログイン | メール+パスワード、サインアップ、Magic Link、Googleログイン |
| `/auth/forgot-password` | パスワード再設定要求 | 再設定メール送信 |
| `/auth/update-password` | 新しいパスワード | 再設定リンク後のパスワード更新 |
| `/auth/session` | セッション | ログイン後の着地点、セッション確認とログアウト |
| `/records` | 記録ハブ | 各記録画面への一覧リンク |
| `/measurements` | 身体測定 | 測定記録・種別・目標・推移グラフ・CSV |
| `/workouts` | 運動・ストレッチ | 概要と当日の予定 |
| `/workouts/exercises` | 種目管理 | 種目の追加・編集・アーカイブ |
| `/workouts/routines` | ルーティン | 種目構成、目標値、複製 |
| `/workouts/schedule` | 予定 | 単発／週次繰り返しの予定作成と状態変更 |
| `/workouts/session/[id]` | セッション実施 | タイマー、種目ごとの実績入力、完了・中止 |
| `/workouts/history` | 履歴 | 実績一覧、週次・月次集計 |
| `/sleep` | 睡眠 | 睡眠・仮眠記録、目標、傾向 |
| `/hydration` | 水分 | 飲み物種別、クイック追加、日別推移 |
| `/condition` | 体調 | 主観指標、症状、体温、メモ |
| `/habits` | 習慣 | 習慣の登録・実施記録・連続記録日数（初期スコープに含む。詳細は5.15節） |
| `/supplements` | サプリメント | 当日の予定と服用記録 |
| `/supplements/products` | 商品管理 | 商品、規格、既定量、低在庫しきい値 |
| `/supplements/schedules` | 摂取予定 | 単発／毎日／週次／必要時 |
| `/supplements/inventory` | 在庫 | ロット、期限、残量 |
| `/supplements/history` | 服用履歴 | 服用・スキップ・取消の履歴とCSV |
| `/meals` | 食事・レシピ | 食品、レシピ、食事記録、献立、栄養集計 |
| `/meals/scan` | 食事画像解析 | 撮影・選択、前処理、解析実行 |
| `/meals/scan/[analysisId]` | 解析レビュー | 候補の修正・確定、食事記録への保存 |
| `/meals/image-history` | 解析履歴 | 過去の解析の閲覧・削除 |
| `/pantry` | 食品在庫・買い物 | 保管場所、ロット、期限、入出庫、買い物リスト |
| `/groceries` | 買い物 | 買い物リスト（在庫画面と同一データ） |
| `/calendar` | カレンダー | 月／週／日／一覧表示、予定CRUD、CSV・ICS出力 |
| `/calendar/integrations/google` | Google連携 | 接続、カレンダー選択、同期方向、手動同期、競合解決、切断 |
| `/notifications` | 通知 | 通知の説明と購読導線 |
| `/notifications/devices` | 端末管理 | 購読端末の一覧と解除 |
| `/notifications/history` | 配信履歴 | 配信結果の確認 |
| `/settings/notifications` | 通知設定 | カテゴリ、静穏時間、1日上限、プレビュー方式 |
| `/settings/account` | アカウント設定 | プロフィール、JSON/CSV出力、健康データ削除、アカウント削除 |
| `/assistant` | AIアシスタント | ストリーミング応答、キャンセル、変更提案の確認・承認、情報提供のみである旨の表示 |
| `/reports` | レポート | 期間・区分・セクション選択、プレビュー、JSON/CSV/印刷 |
| `/reports/print/[reportId]` | 印刷表示 | A4印刷（PDF化はブラウザ機能） |
| `/offline` | オフライン | 接続断時のフォールバック画面 |

補助ルート: `/error.tsx`、`/loading.tsx`、`/not-found.tsx`、`/manifest.webmanifest`。

> version2では、旧実装で「設計のみで未実装」とされていたダッシュボード（`/`）・習慣（`/habits`）・カレンダーICS出力・AI変更提案の承認フローUIを、**初期スコープに含める**（詳細は5.14〜5.17節）。

---

## 5. 機能要件

### 5.1 認証・アカウント

- 提供する認証手段: **メール+パスワード登録／ログイン**、**Magic Link**、**Googleログイン（OAuth）**、**パスワード再設定**、**ログアウト**。
- パスワードは8文字以上256文字以内、確認用パスワードとの一致を必須とする。
- メール確認は `/auth/confirm`、OAuth／Magic Linkのコールバックは `/auth/callback` で処理する。
- ログイン後の遷移先 `next` は**アプリ内の相対パスのみ許可**し、`//`、バックスラッシュ、NUL、外部オリジンを含む値は既定の `/auth/session` へ丸める（オープンリダイレクト対策）。
- Googleログインは認証に必要な最小スコープのみを要求し、**Google Calendarの権限同意とは完全に分離**する。
- エクスポートとアカウント削除は**直近の再認証**を要求する。
- 利用者状態（`users.status`）が `active` 以外（`suspended` / `health_data_erasure_pending` / `deletion_pending`）の場合、有効なJWTが残っていても全APIが HTTP 403（`ACCOUNT_INACTIVE`）を返す。

#### データ出力・削除

| 操作 | 内容 |
|---|---|
| データ出力 | `GET /api/account/export?format=json|csv`。所有者範囲の全テーブルを出力。1テーブル25,000行、合計100,000行、ページサイズ500行の上限を持ち、`Content-Disposition: attachment` と `no-store` を付与する |
| 健康データ削除 | `DELETE /api/account/data`。identityを `health_data_erasure_pending` にして新規書き込みを止め、Google接続のrevoke → Storage実体削除 → 健康データ削除RPCの順で実行し、最後にactiveへ戻す。アカウント自体は保持 |
| アカウント削除 | `DELETE /api/account`。`deletion_pending` へ遷移後、Google接続revoke、Storage削除、Auth Admin APIによるユーザー削除、CASCADE、セッション破棄まで実行する |

いずれも same-origin 検証、再認証、署名済みセッション由来のUID固定を経て実行し、リクエストボディの所有者IDは使用しない。削除中断時は pending のまま安全に再試行できる。

### 5.2 プロフィール／初回セットアップ

- `/onboarding`（認証時）と `/demo`（デモ時）で同一の `ProfileWorkspace` を用いる。
- 提示する候補項目: 表示名、年齢、性別（女性／男性／ノンバイナリー／自己記述／回答しない）、身長、現在体重、目標体重、へそ周り、骨盤周り、太もも周り、肩幅、体型目標、運動候補、朝食候補、目標期限、運動可能曜日、食生活メモ、体調メモ、睡眠目標時間、単位系（metric/imperial）、タイムゾーン。
- **候補は編集可能で、`confirmed: true` を伴う確認操作の後にのみ保存する**。
- 検証: 年齢1〜130、身長30〜300cm、体重10〜500kg、周囲径10〜500cm、目標期限は選択タイムゾーンの当日以降、タイムゾーンはIANA名として実在すること、運動可能曜日の重複禁止、自己記述の性別は説明必須。
- 認証時は `save_my_confirmed_profile(profile jsonb)` RPCで、`users.timezone`・`user_profiles.settings.confirmed_profile`・体重目標を同一トランザクションで保存する（部分保存が起きない）。
- 過度な減量や極端な摂取制限を推奨しない文言を用いる。

### 5.3 身体測定

- 既定項目: 体重、体脂肪率、BMI、ウエスト、へそ周り、骨盤周り、ヒップ、太もも、ふくらはぎ、肩幅。任意のカスタム項目キー（`^[a-z][a-z0-9_]{1,49}$`）も追加できる。
- 単位: `kg` / `lb` / `cm` / `inch` / `percent` / `custom`。項目種別に応じた単位制約（体重は kg|lb、体脂肪率・BMIは %、周囲・長さは cm|inch）を持つ。
- 記録項目: 日付、値（0超1000以下）、単位、メモ（500字）、測定条件、測定部位、写真参照（HTTPS URL または `storage://health-images/<uuid>/...` 形式のみ許可）。
- 計算: BMI（`体重kg / (身長m)^2` を小数1桁）、`kg↔lb`（0.45359237）、`cm↔inch`（2.54）の相互変換。
- 表示: 推移グラフ、目標線、移動平均などのトレンド、CSV出力。
- 測定種別と測定目標は `/api/measurements/types`、`/api/measurements/goals` で管理し、既定種別は `seed_default_body_measurement_types` RPCで投入する。
- 同一の所有者・種別・日時の重複登録はDBの一意制約で防ぐ。

### 5.4 運動・ストレッチ

- **種目**: 種別（workout / stretch）、カテゴリ（ウォーキング、ランニング、有酸素、自重、筋力、ストレッチ、ウォームアップ、クールダウン、その他）、記録方式（時間／距離／回数／セットと回数／重量と回数／保持時間／左右別保持時間／完了のみ）、既定単位、説明、安全上の注意、既定種目フラグ。既定種目はアーカイブ不可。有効な種目間で名称正規化（NFKC・小文字化）またはstableKeyの重複を禁止する。
- **ルーティン**: 種別（workout / stretch / mixed）、想定所要時間（1〜1440分）、1〜100件の種目項目。項目ごとに目標時間・距離・セット・回数・重量・保持時間・休憩秒数・左右別・必須フラグを持つ。同一種目の重複追加は不可。複製可能。
- **予定**: ルーティンまたは単一種目のいずれか一方を指定し、日時・IANAタイムゾーン・単発／週次繰り返し（曜日集合）・状態（scheduled / in_progress / completed / skipped / cancelled）を持つ。指定期間へ展開して表示する。
- **セッション**: 状態遷移は `in_progress ⇄ paused → completed | aborted` のみ許可し、完了・中止には終了日時を必須とする。終了日時が開始日時より前の値は拒否する。種目ごとに実績（時間、距離、セット、回数、重量、保持時間、左右別秒数、完了／スキップ、メモ）と主観的運動強度（1〜10）、体調メモを記録する。同一種目に完了とスキップを同時指定できない。
- **タイマー**: 実施用のストップウォッチ・休憩タイマー。
- **集計**: 週次・月次の実施回数と合計時間、カテゴリ別時間、当月の予定消化率。

### 5.5 睡眠・水分・体調

- **睡眠**: 種別（夜間／仮眠／その他）、就床・入眠・起床・離床の各日時、タイムゾーン、中途覚醒回数（0〜30）、覚醒時間（0〜720分）、睡眠の質（1〜5）、起床時の感覚（1〜5）、メモ。就床≦入眠＜起床≦離床の順序、24時間超や覚醒時間が睡眠時間以上となる値を拒否する。睡眠時間は `起床-入眠-覚醒時間` で算出する。
- **水分**: 記録日時、飲み物名、単位（`ml` / `l` / `us_fl_oz`）、量（0超10,000以下）、カフェイン有無、アルコール有無、メモ。集計用に `ml` 正規化値を保持する。既定の飲み物候補10種を提供し、任意の種別を追加できる。
- **体調**: 総合・疲労・活力・ストレス・痛み・気分（各0〜10）、体温（30〜45℃）、症状（既定13種＋任意30件まで）、自由記述症状（10件まで）、メモ。
- **目標**: 睡眠・水分の目標量、対象曜日、目標就床・起床時刻、開始日・終了日を設定できる。
- 平均値算出、CSV出力（数式インジェクション対策として `=+-@` 始まりの値へ `'` を前置）を提供する。比較結果を因果関係として断定しない。

### 5.6 サプリメント

- **商品**: 名称、ブランド、カテゴリ（ビタミン、ミネラル、プロテイン、アミノ酸、食物繊維、プロバイオティクス、植物性、その他）、剤形（錠剤、カプセル、粉末、液体、グミ、顆粒、その他）、既定量と単位、容器あたり量、低在庫しきい値、成分メモ、安全上の注意、HTTPS URL。名称正規化またはstableKeyの重複を禁止する。
- **摂取予定**: 単発／毎日／週次／必要時、時刻（HH:MM）、タイムゾーン、曜日、開始日・終了日、量・単位、食事との関係（指定なし／食前／食中／食後／表示に従う）。終了日は開始日以降、週次は曜日必須。
- **服用記録**: 状態（服用／スキップ／取消／必要時）、予定発生日時、記録日時、量、在庫消費量、**冪等キー（8〜200文字）**。取消（void）に対応する。
- **在庫**: ロット単位で数量、購入日、開封日、使用期限、残量を管理。服用時は期限が近いロットから消費（FEFO）し、負在庫となる操作は原子的RPC（`record_supplement_intake` / `void_supplement_intake`）が拒否する。
- **集計**: 週の予定数と服用数、月の服用数、低在庫商品数、期限接近ロット数。CSV出力可。
- 相互作用や安全性の確定判断は行わず、必要時は専門家への相談を案内する。

### 5.7 食事・レシピ・献立

- **食品**: 名称、ブランド、カテゴリ16種、基準量と基準単位（g / mL / 個 / 枚 / カップ / 人前 / パック / 大さじ / 小さじ / その他）、単位あたりグラム、栄養素（エネルギー、たんぱく質、脂質、炭水化物、食物繊維、食塩相当量）、アレルゲン（既定8種＋その他、30件まで）、栄養値の出所（表示ラベル／推定／手入力）、HTTPS URL、アーカイブ。
- **レシピ**: 名称、説明、カテゴリ11種、出来上がり人数、調理時間（0〜10,080分）、手順（5,000字）、材料（最大100件）。栄養値は材料と食品データから計算し、**単位換算できない材料や栄養値が欠けた材料がある場合は「不完全」と明示**して捏造しない。
- **食事記録**: 日時、タイムゾーン、区分（朝食／昼食／夕食／間食／夜食／その他）、名称、状況、空腹度（1〜5）、満足度（1〜5）、メモ、明細（食品／レシピ/手入力、最大100件、栄養スナップショット付き）、**冪等キー**。削除は論理削除。
- **献立**: 予定日時、区分、レシピ参照、繰り返し曜日、期間、状態（planned / converted / skipped / cancelled）。`convert_meal_plan` RPCで食事記録へ変換し、冪等キーによって二重変換を防ぐ。
- **集計・出力**: 当日・7日・30日の栄養集計、画面上の表形式代替表示、CSV出力（数式インジェクション対策あり）。
- 栄養値は推定であることを表示する。

### 5.8 食事画像解析

- 対応入力: JPEG / PNG / WebP、6MB以下、1200万画素以下。HEIC/HEIFは非対応であることを明示的に案内する。スマートフォンでは `capture` によりカメラ直接撮影が可能。
- クライアント前処理: 最大辺1600pxへ縮小し、Canvasで**JPEG（品質0.82）へ再エンコードしてEXIF/GPS等のメタデータを除去**、SHA-256でコンテンツハッシュを算出して重複解析を抑止する。
- サーバー再検証: マジックバイト、MIME、拡張子、寸法、バイト数、ハッシュを再検査する（`src/server/food-image/validation.ts`）。
- プロバイダー: `gemini`（無料枠のみ）／`ollama`（ループバック限定のローカル）／`manual`（手入力）／`fake`（決定論的テスト用）。有料・非許可モデル、OpenAI／Anthropic、任意URL指定、自動有料フォールバックはいずれも拒否する。
- **外部送信には版付きの明示同意（`food-image-external-v1`）が必要**で、同意がない場合も手入力経路は常に利用できる。
- 解析結果の契約: 項目ごとに元ラベル、正規化ラベル、信頼度（0〜1）、推定量と単位（量と単位は必ず対で）、量の信頼度、調理法、可視材料、不確実性の理由、候補検索語を返し、全体信頼度、警告、画質メモ、非食品検出、要手動確認フラグを持つ。最大12件。
- 保存フロー: 候補を編集・削除・追加でき、既存食品とのマッチングを提示する。**栄養値は既存の食品データからのみ計算**し、未知項目は不明のまま扱う。明示的な保存確認の後にのみ食事記録を作成し、作成時に解析を `confirmed` として食事記録IDへ紐付ける（`reviewing` 状態からのみ遷移可能）。
- 保持: 既定は `ephemeral`（保持24時間、バケット `food-images-private`）。元画像バイト列、公開URL、プロバイダーの生応答は既定フローで永続化しない。1日あたりの解析回数上限（既定20件）を持つ。
- 履歴は所有者範囲で閲覧・削除できる。

### 5.9 食品在庫・買い物

- **保管場所**: 名称、種別（冷蔵／冷凍／野菜室／常温／棚／調味料棚／その他）、並び順、有効・アーカイブ。
- **在庫ロット**: 食品参照、保管場所、ラベル、初期量・現在量、単位、単位あたりグラム、購入日、入庫日、開封日、期限種別（なし／賞味期限／消費期限／任意）と期限日、低在庫しきい値、価格・通貨（既定JPY）、メモ。期限種別と期限日の整合、開封日≧入庫日を検証する。
- **入出庫取引**: 種別（入庫／使用／調整増／調整減／廃棄／取消／買い物購入）、量、単位、発生日時、関連買い物項目・食事、**冪等キー**、取消元ID。`record_pantry_transaction` RPCが原子的に在庫を更新し、負在庫を拒否する。取消により在庫を戻せる。
- **買い物リスト**: 名称、状態（下書き／買い物中／完了／アーカイブ）、予定日、店舗名。項目は名称、量、単位、状態（未購入／購入済み／スキップ／不要／在庫反映済み）、優先度（0〜3）、カテゴリ、発生元（手動／低在庫／在庫切れ／レシピ／献立／その他）、購入量・購入単位・購入日時、冪等キー、並び順を持つ。購入品を在庫へ反映できる。
- **表示・集計**: 期限ラベル、食品数、ロット数、低在庫数、在庫切れ数、3日以内期限数、期限超過数。レシピ必要量に対する不足量表示（単位換算不能な場合は明示）。CSV出力。
- **食品の安全性を期限だけで判断せず、自動廃棄や摂取推奨を行わない**。

### 5.10 カレンダー（アプリ内）

- 表示: 月／週／日／一覧（agenda）、24時間表記。
- 手動予定: タイトル、説明、開始・終了日時、終日、タイムゾーン、色（`#rrggbb`）、カテゴリ、状態（予定／完了／取消）、プライバシー水準（private / standard）、繰り返し（なし／毎日／毎週／毎月）と繰り返し終了日時、通知分前（0〜43,200）、アーカイブ。終了は開始より後であることを必須とする。
- 繰り返し例外: 発生ごとの一意キー（`<eventId>:<ISO開始日時>`）に対し、スキップまたは上書き（時間帯・タイトル・説明・状態）を最大500件設定できる。1発生につき例外は1つ。展開は最大2,000回で打ち切る。
- 読み取り専用ソース: 運動、サプリ、食事、睡眠、水分、体調、測定、在庫、Google Calendar 由来の予定を表示のみで統合する。**編集・削除できるのは `manual` ソースのみ**で、元の記録だけが編集可能な唯一の情報源として残る。
- 絞り込み: ソース別・状態別。
- CSV出力: 14列（event_id, occurrence_key, title, description, start, end, all_day, timezone, source, source_id, category, status, privacy_level, recurrence）。プライバシーモードではタイトルを `Private event` に置換し説明を除去する。数式インジェクション対策あり。
- ICS出力（version2で新規追加。詳細5.16節）。

### 5.11 Google Calendar連携

- OAuth: サーバー側ルートによる Authorization Code + PKCE。stateはHttpOnly/SameSite Cookieに保持し、有効期限10分、定数時間比較で検証、コールバック後に破棄する。戻り先は `/calendar` 配下の許可リストのみ。
- スコープ: 読み取りは `calendar.readonly`。**書き出し（エクスポート）を明示的に有効化した場合にのみ** `calendar.events` を要求する。
- トークン: `GOOGLE_CALENDAR_TOKEN_ENCRYPTION_KEY`（base64url 32バイト）による AES-256-GCM（nonce・認証タグ・鍵バージョン付き）で `private.google_oauth_tokens` に保存。鍵未設定・改ざん時は安全に失敗し、トークンをUI・API・localStorage・IndexedDBへ返さない。
- 同期: カレンダー選択、フル同期／増分同期（syncToken、ページング、原子的カーソル前進、`410 Gone` 時のフル再同期）、取消イベントのキャッシュ更新、アプリ→Google のエクスポート（Google拡張プロパティによる決定論的冪等キー、部分保存後のマッピング復旧、更新・取消処理）。
- 競合: 永続化し、「アプリ優先」「Google優先」「両方保持」「後で対応」の4操作で解決する。
- 切断: サーバー側でGoogleのトークンをrevokeし、その成否を記録してから公開接続行を削除する（private側だけが先に消える順序を避ける）。クライアントから直接DELETEできない。
- ローカル／テスト用に Fake Provider（`GOOGLE_CALENDAR_PROVIDER=fake`）を持ち、本番設定では拒否する。
- Google由来イベントは読み取り専用として表示し、`visibility=private` のイベントはタイトル・説明を伏せる。終日イベントの排他的終了日境界を保持する。

### 5.12 通知（Web Push）

- オプトイン方式。説明後の利用者操作からのみ通知権限とVAPID購読を要求する。ホーム画面追加が必要な環境や非対応環境の案内を表示する。
- 設定: 有効/無効、タイムゾーン、静穏時間（既定22:00〜07:00）、1日の最大通知数（1〜20、既定8）、プレビュー方式（`private` 既定 / `standard`）、カテゴリ選択（運動、サプリ、食事、睡眠、水分、測定、在庫、カレンダーの8種、最低1つ）。
- 端末管理: 購読端末の一覧（endpoint末尾12文字のみを表示用識別子として使用）と解除。配信履歴の閲覧。
- 保存: 公開側にはendpointのSHA-256ハッシュと端末状態のみを置き、endpoint・`p256dh`・auth secretは `private.push_subscription_secrets` に保存する。endpointハッシュは全アカウントで一意で、別アカウントへの再割り当ては同一サーバー処理内で旧行と秘密値を削除してから登録する。
- 配信: `notification_jobs` によるアウトボックス方式。`POST /api/notifications/dispatch` は `CRON_SECRET` を定数時間比較で検証するスケジューラー専用エンドポイントで、ブラウザセッションからは実行できない。停止中クレームの回復、上限付き再試行、静穏時間を考慮した再スケジュール、Push応答 `404`/`410` の購読削除を行う。
- Service Worker: `push` ハンドラでタイトル120字・本文500字に丸め、`notificationclick` の遷移先は**自オリジンの絶対パスのみ**許可（不正値は `/notifications`）。壊れたペイロードでもSWを壊さない。

### 5.13 AIアシスタント

- `/assistant` でストリーミング応答とキャンセルを提供し、情報提供のみである旨の安全表示を常時行う。
- ルーティング既定は `AI_COST_MODE=zero-cost`: 許可リスト内のGemini無料枠モデル → 明示オプトインのローカルOllama → 決定論的フォールバックの順。**OpenAI・Anthropicはこのモードで常に拒否**する。
- コンテキストは直近12件（最大40件）、リクエストタイムアウト既定20秒。
- 変更提案の確認・承認フロー（version2で新規追加。詳細5.17節）。

### 5.14 ダッシュボード（version2で新規追加）

- `/`は未認証時はサービス紹介、認証済み時は以下を表示するダッシュボードとする。
  - 最新の身体測定値（体重・BMI）と目標との差
  - 直近の運動実績・当日の予定
  - 当日の食事記録状況（記録済み区分）
  - 当日のサプリメント摂取状況
  - 連続記録日数（habitsまたは主要記録の継続日数）
  - クイック操作（測定・運動・食事・水分の即時記録への導線）
- 表示データはすべて所有者範囲のもので、他ユーザーのデータを含まない。
- モバイルでは表示件数・情報量を絞り、重要度の高いものから表示する。

### 5.15 習慣（habits）（version2で新規追加）

- 習慣: 名称、頻度（毎日／週次曜日指定／任意）、リマインダー時刻、有効・アーカイブ。
- 習慣実施記録: 実施日、状態（実施／スキップ）、メモ。
- 連続記録日数（streak）の算出、週次・月次の達成率表示。
- 未達成を責める表現・罰する表現を用いない（10章の健康上の安全要件に従う）。

### 5.16 カレンダーICS出力（version2で新規追加）

- `/calendar` から選択範囲・ソースに基づき iCalendar（.ics）形式で出力できる。
- プライバシーモードの適用（タイトル置換・説明除去）はCSV出力と同様の規則に従う。
- 生成するVEVENTには最低限 UID・DTSTART・DTEND・SUMMARY・（該当時）RRULEを含め、5.10節の繰り返し・例外ルールと整合させる。

### 5.17 AI変更提案の承認フロー（version2で新規追加）

- AIアシスタントが健康データの変更（測定目標の更新、予定の追加など）を提案する場合、`ai_change_proposals` に提案内容を記録し、`/assistant` 上でプレビュー表示する。
- 利用者が内容を確認し明示的に承認した場合のみ、対応する機能APIを通じて実データへ反映する（5.13節の承認主義に従い、AIが直接テーブルを書き換えない）。
- 却下・保留も選択でき、保留分は一覧で再確認できる。

---

## 6. データモデル

### 6.1 全体像

Supabase PostgreSQL にmigrationで構築する。テーブルは機能領域ごとに`public`スキーマへ配置し、秘密値は`private`スキーマに分離する。

| 境界 | 主なテーブル |
|---|---|
| ID・プロフィール | `users`, `user_profiles`, `health_goals`, `uploaded_images`, `audit_logs` |
| 身体測定 | `body_measurement_types`, `body_measurements`, `body_measurement_goals`, `progress_photos` |
| 運動・ストレッチ | `exercise_types`, `workout_routines`, `workout_routine_items`, `workout_schedules`, `workout_sessions`, `workout_session_items` |
| 睡眠・水分・体調 | `sleep_entries`, `sleep_goals`, `hydration_entries`, `hydration_goals`, `beverage_types`, `condition_entries`, `condition_entry_symptoms`, `symptom_types` |
| 習慣 | `habits`, `habit_logs` |
| サプリメント | `supplement_products`, `supplement_schedules`, `supplement_intake_logs`, `supplement_inventory_lots`, `supplement_inventory_movements` |
| 食事・レシピ・献立 | `meal_food_items`, `meal_recipes`, `meal_recipe_ingredients`, `meal_recipe_entries`, `meal_recipe_entry_items`, `meal_recipe_plans` |
| 在庫・買い物 | `pantry_locations`, `pantry_inventory_lots`, `pantry_inventory_transactions`, `grocery_lists`, `grocery_list_items` |
| カレンダー | `calendar_events`, `calendar_event_exceptions`, `calendar_sync_mappings` |
| Google Calendar | `google_calendar_connections`, `google_calendar_selections`, `google_calendar_sync_states`, `google_calendar_imported_events`, `google_calendar_sync_conflicts`, `google_calendar_sync_operations` |
| 通知 | `notification_preferences`, `push_subscriptions`, `notification_jobs`, `notification_deliveries`, `notification_rules` |
| AI | `ai_conversations`, `ai_messages`, `ai_runs`, `ai_tool_calls`, `ai_source_references`, `ai_change_proposals`, `ai_action_proposals`, `ai_action_executions`, `ai_user_preferences`, `ai_external_data_consents`, `ai_safety_events`, `ai_usage_daily` |
| 食事画像 | `food_image_consents`, `food_image_analyses`, `food_image_usage_daily` |
| レポート | `report_templates`, `report_exports`（version2では永続化して出力する。第14章参照） |
| オフライン同期 | `offline_sync_operation_results` |
| サーバー秘密値 | `private.google_oauth_tokens`, `private.push_subscription_secrets`, `private.ai_usage_counters` |

version2では、旧実装に存在した初期版テーブル（`food_items`, `meal_logs`, `recipes`, `pantry_items`, `sleep_logs` 等の後継テーブルに置き換わった旧テーブル群）は**作成しない**。最初から現行テーブルのみで構築する。

### 6.2 ID・所有権の規則

- IDはUUID（原則 `gen_random_uuid()`）。
- 全ユーザーデータに `owner_id uuid not null` を持たせ、全公開テーブルは `(id, owner_id)` の候補キーを持つ。
- 親子参照は単独IDではなく **`(parent_id, owner_id) -> (id, owner_id)` の複合外部キー**とし、他ユーザーの親へ子を接続できないようにする。
- `public.users.id` は `auth.users.id` を参照し、`owner_id = id` をCHECK制約で固定する。
- `auth.users` の AFTER INSERT トリガー `on_auth_user_created`（`SECURITY DEFINER SET search_path = ''`）が `public.users` と `public.user_profiles` を同一UUIDで作成する。
- よく使う時系列検索は `(owner_id, timestamp)`、期限検索は `(owner_id, expiry)` の複合インデックスを持つ。

### 6.3 日時・単位

- 瞬間は `timestamptz`、日単位は `date`、ローカル時刻は `time` + タイムゾーンで保存する。
- 表示タイムゾーンはIANA名で保持し、既定は `Asia/Tokyo`。
- 重量 `kg`/`lb`、長さ `cm`/`inch`、水分 `ml`/`l`/`us_fl_oz` を入力値・入力単位のまま保存し、集計用の正規化値（水分は `amount_ml`）を併せて持つ。
- 栄養値・消費量は推定フラグを持ち、正確値として扱わない。

### 6.4 版番号・冪等性

- `audit_logs` を除く可変の公開テーブルは `row_version bigint not null default 1`、`client_mutation_id uuid`、`(owner_id, client_mutation_id)` のNULL除外一意インデックスを持つ。
- 更新は `WHERE id = :id AND owner_id = auth.uid() AND row_version = :expected` の楽観ロックで行い、更新0件を競合（HTTP 409）として扱う。
- INSERTトリガーがクライアント入力によらず `row_version = 1` とサーバー時刻を強制し、UPDATEトリガーが `id` / `owner_id` / `created_at` の変更を拒否する。

### 6.5 書き込み権限の例外（server-only）

| テーブル | authenticatedに許可する操作 | 実際の書き込み主体 |
|---|---|---|
| `users` | SELECT、ロケール／タイムゾーン／最終利用日時のUPDATE | Auth連携サーバー |
| `uploaded_images` | SELECT | 画像検査API |
| `google_calendar_connections` / `calendar_sync_mappings` | SELECT | OAuthコールバック・同期サービス |
| `push_subscriptions` | SELECT、DELETE | Push登録API |
| `notification_deliveries` | SELECT | スケジューラー／配信ワーカー |
| `ai_change_proposals` | SELECT、承認/却下操作を経由したUPDATE | 構造検証済みAIサービス／承認済みAPI |
| `ai_messages` | SELECT、DELETE、`role='user'` のINSERT/UPDATE | AIサービス |
| `audit_logs` | SELECT | サーバー側監査ライター |
| `private.*` | 権限なし | service roleのみ |

全公開テーブルのポリシーは所有者条件に加えて `public.is_active_user()` を要求し、`suspended` / `health_data_erasure_pending` / `deletion_pending` の利用者を全操作から排除する。`service_role` はブラウザへ渡さない。

### 6.6 ストレージ

- 非公開バケット `health-images`（最大10MiB、JPEG/PNG/WebP/HEIC/HEIF）と `food-images-private` の2つ。いずれも匿名アクセス不可。
- オブジェクトパスは `<auth.uid()>/<random-uuid>.<検証済み拡張子>` に固定し、Storageオブジェクトとメタデータ行の双方で所有者を検査する。公開URLは保存しない。

---

## 7. API仕様

すべてのRoute Handlerは、状態変更時に **same-origin検証** と **`Content-Type: application/json` の要求**、**リクエストボディ64KiB上限**、`Cache-Control: no-store` の応答ヘッダーを適用する。所有者は常に検証済みセッションから導出する。

| エンドポイント | メソッド | 概要 |
|---|---|---|
| `/api/health` | GET | 内容を持たない公開の到達性確認（`{ ok: true }`、`no-store`）。PWAの接続判定に使用 |
| `/api/account/profile` | GET / PATCH | 確定プロフィールの取得・更新 |
| `/api/account/export` | GET | JSON/CSVでのアカウントデータ出力（要再認証） |
| `/api/account/data` | DELETE | 健康データのみ削除（要再認証） |
| `/api/account` | DELETE | アカウント削除（要再認証） |
| `/api/measurements` | GET / POST / DELETE | 身体測定の取得・保存・削除 |
| `/api/measurements/types` | POST | 測定種別の追加・既定投入 |
| `/api/measurements/goals` | GET / POST / DELETE | 測定目標 |
| `/api/workouts` | GET / POST | 種目・ルーティン・予定・セッション |
| `/api/wellness` | GET / POST | 睡眠・水分・体調・目標・種別 |
| `/api/habits` | GET / POST | 習慣・実施記録（version2新規） |
| `/api/supplements` | GET / POST | 商品・予定・ロット・服用 |
| `/api/meals` | GET / POST | 食品・レシピ・食事・献立 |
| `/api/pantry` | GET / POST | 保管場所・ロット・買い物リスト・項目・取引 |
| `/api/calendar` | GET / POST | アプリ内予定の保存・削除 |
| `/api/calendar/export` | GET | ICS出力（version2新規） |
| `/api/calendar/google` | GET / POST | 状態取得とキャッシュ、選択・同期・エクスポート・競合解決・切断 |
| `/api/calendar/google/connect` | GET | OAuth開始（PKCE、state Cookie発行） |
| `/api/calendar/google/callback` | GET | OAuthコールバック（state検証、トークン暗号化保存） |
| `/api/food-image/consent` | GET / POST | 外部送信同意の取得・登録 |
| `/api/food-image/analysis` | GET / POST / DELETE | 解析実行・履歴取得・削除 |
| `/api/notifications` | GET / POST | 設定と購読 |
| `/api/notifications/dispatch` | POST | スケジューラー専用配信（`CRON_SECRET` 必須） |
| `/api/assistant` | POST | AI応答のストリーミング（キャンセル対応） |
| `/api/assistant/proposals` | GET / POST | 変更提案の一覧・承認・却下（version2新規） |
| `/api/reports` | POST | レポート生成（preview / json / csv） |
| `/api/sync` | GET / POST | オフラインミューテーションの適用 |
| `/auth/callback` | GET | OAuth / Magic Link のコード交換 |
| `/auth/confirm` | GET | メール確認 |

エラー応答は `{ error: { code, message } }` 形式で、主なコードは `AUTHENTICATION_REQUIRED`（401）、`ACCOUNT_INACTIVE`（403）、`SAME_ORIGIN_REQUIRED`（403）、`JSON_REQUIRED`（415）、`*_CONFLICT`（409）、`ACCOUNT_SERVICE_UNAVAILABLE`（503）。

---

## 8. オフライン同期・PWA要件

### 8.1 ミューテーションキュー

- IndexedDB `health-web-app-offline-sync-v1` に、所有者ID・エンティティ種別・操作種別（create / update / delete / restore）・冪等キー・基準行バージョン・試行回数・期限を持つキューを保存する。
- 制限値: 所有者あたり最大200件、1ミューテーション48KiB、TTL 30日、クレームTTL 45秒、最大試行8回。
- 再試行は指数バックオフ（上限60秒）＋ジッターで行い、`Retry-After` を尊重する（上限1時間）。
- 状態: `pending` / `claimed` / `syncing` / `succeeded` / `failed` / `conflict` / `cancelled` / `expired`。期限切れリースの回復、create+updateの圧縮、create+deleteの整理を行う。
- **ペイロード境界**: `owner_id` / `user_id` / `token` / `secret` / `api_key` / `authorization` に一致するキー、プロトタイプ汚染キー（`__proto__` 等）、12段を超える入れ子、過大サイズ、任意エンドポイント指定を拒否する。所有権は常にセッションから導出する。
- 競合（HTTP 409）応答は `{ ok: false, error: { code: "SYNC_CONFLICT" }, conflict: {...} }` の固定契約で、サーバー行の実バージョン、更新日時、変更フィールド、サーバー側削除有無を返す。自動上書きせず、利用者に提示する。
- 同一冪等キーの再送は `offline_sync_operation_results` により重複適用されない。
- version2では初期スコープから全機能エンティティを対象とする（旧実装で「測定のみ対応」だった制約は設けない）。

### 8.2 Service Worker（`public/sw.js`）

- キャッシュ名を版管理し、旧キャッシュを `activate` で削除する。
- **キャッシュ対象は許可リストのみ**: `/_next/static/`、`/icons/`、`/manifest.webmanifest` の style / script / image / font。クエリ付きURLは対象外。
- `/api/`、`/auth/`、GET以外、クロスオリジンは常にネットワーク直行（キャッシュしない）。
- ナビゲーション失敗時は `/offline` を返す。オフライン起動用のアプリシェルキャッシュ対象画面は実装フェーズで確定する（利用者データは含まない）。
- `SKIP_WAITING` による更新適用と、`CLEAR_PRIVATE_DATA` によるアプリ所有キャッシュの一括削除に対応する。ログアウト、健康データ削除、アカウント削除、手動リセット時にIndexedDBとCache Storageを消去し、他オリジンの保存領域には触れない。
- `push` / `notificationclick` を実装する（5.12節参照）。
- オンラインイベントのみに依存しないヘルスチェックと劣化表示を持つ。

### 8.3 マニフェストとiPhone対応

- `name: Health Web App` / `short_name: Health` / `display: standalone` / `orientation: portrait` / `start_url: /` / テーマ色 `#267a5b`、背景色 `#f6f8f5`。
- アイコン: 192px、512px、maskable 512px（SVG）。Apple Touch Icon を併せて指定。
- `viewport-fit: cover` によるsafe area対応、`appleWebApp` 設定、ライト／ダークのテーマ色を宣言する。
- 電話番号の自動検出を無効化する。

---

## 9. セキュリティ・プライバシー要件

### 9.1 ヘッダー（`next.config.ts`、全ルートに適用）

| ヘッダー | 値 |
|---|---|
| Content-Security-Policy | `default-src 'self'; base-uri 'self'; frame-ancestors 'none'; form-action 'self'; img-src 'self' data: blob: https:; connect-src 'self' https://*.supabase.co https://generativelanguage.googleapis.com; script-src 'self' 'unsafe-inline'; style-src 'self' 'unsafe-inline'; worker-src 'self' blob:; manifest-src 'self'; object-src 'none'` |
| Referrer-Policy | `strict-origin-when-cross-origin` |
| X-Content-Type-Options | `nosniff` |
| X-Frame-Options | `DENY` |
| Permissions-Policy | `camera=(self), geolocation=(), microphone=(), payment=(), usb=()` |
| Cross-Origin-Opener-Policy / Resource-Policy | `same-origin` |

### 9.2 サーバー境界

- サービスロール鍵、OAuthトークン、AI APIキー、VAPID秘密鍵をクライアントバンドルへ含めない。`NEXT_PUBLIC_` を付けてよいのは公開可能な設定のみ。
- 全入力をZodで検証し（`.strict()` で未知フィールドを拒否）、DB制約とRLSを最終防衛線とする。SQLはパラメータ化されたSDKまたは管理済みDB関数のみで実行する。
- 状態変更APIは same-origin、Content-Type、認証、利用者状態、ボディサイズを検証する。
- アップロードは拡張子だけでなく実体シグネチャ・MIME・寸法・サイズ・ハッシュを検証し、メタデータを除去する。
- 出力をHTML文字列として挿入しない。CSVは数式インジェクション対策（`=+-@` 前置クォート）を全経路で適用する。
- 健康データ、画像、秘密値、OAuthトークンをログへ出力しない。重要操作は `audit_logs` に記録する（内容を持たない最小イベント）。
- 破壊的な削除RPCは `service_role` 限定で、PUBLIC / `anon` / `authenticated` からEXECUTE権限を剥奪する。

### 9.3 データ保持・削除

- 食事画像は既定で一時保持（24時間）。元画像・EXIF・公開URL・プロバイダー生応答を永続化しない。
- レポート出力は永続化する場合も外部送信・公開URL・メール配信を行わない（第14章参照）。
- 健康データ削除はアカウントを保持したまま健康・予定・連携・AIデータと秘密値を削除し、`user_profiles` を初期化する。
- 削除・出力の範囲と外部送信先を画面と文書で説明する。

---

## 10. 健康上の安全要件

- 医療機器ではなく、診断・治療を行わない旨をトップページとAIアシスタントに明示する。
- 診断、治療、投薬変更、サプリメントの安全性、部分痩せ、推定栄養値を断定しない。
- 食品の安全性を期限だけで判断せず、自動廃棄や摂取推奨を行わない。
- 相関を因果として断定せず、未達成を責める表現・罰する表現を用いない（習慣機能を含む）。
- 相互作用や体調不良に関する判断が必要な場面では、医師・薬剤師など専門家への相談を案内する。

---

## 11. UI・アクセシビリティ要件

- 下部固定ナビゲーション（AI／ホーム／カレンダー／記録／食事／レポート／その他）に `aria-label` と `aria-current` を付与する。
- 「本文へ移動」スキップリンクと `id="main-content"` を全ページに置く。
- 44px以上を目安とする操作領域、片手で届く位置への主要操作配置。
- ラベル、具体的な入力エラー、ローディング、空状態、エラー、オフライン、同期中の各状態を表示する。
- ライト／ダークテーマ、十分なコントラスト、キーボード操作、フォーカス表示、`prefers-reduced-motion` に対応する。
- グラフには常に表形式の代替表示を用意する。
- 動作しないボタンや到達不能な画面を残さない。

---

## 12. 品質・テスト・CI要件

| 種別 | 内容 |
|---|---|
| 静的検査 | `npm run typecheck`（TypeScript strict）、`npm run lint`（ESLint）、`npm run format`（Prettier） |
| 単体・結合 | `npm test`（Vitest）。スキーマ検証、単位換算、集計、同期キュー、Service Worker、設定検証、プロバイダー境界などを対象とする |
| DB | PGliteへのmigration適用検証、スキーマ契約テスト、Storage境界テスト。Docker利用時は `supabase db reset` / `db lint` / `supabase test db`（pgTAP）でRLSと所有者分離を検証する |
| E2E | Playwright（Chromium / Mobile Safari）。測定、運動、食事、在庫、体調、カレンダー、Google連携、通知、AI、画像解析、レポート、習慣、ダッシュボード、PWA、オフライン→オンライン往復の各シナリオ |
| ステージング | 受け入れテストとステージング検証スクリプト。合成の一時IDによる所有者A/B分離、Storage署名URL分離、匿名拒否を検証する |
| 環境検証 | `npm run env:check`（ローカル）、`env:check:production`、`release:check`（リリース成果物と秘密値監査） |
| CI | push / PR / 手動トリガーで環境検証 → リリース検証 → typecheck → lint → テスト → 本番ビルド → E2E を実行 |
| 依存管理 | バージョンとlockfileを固定し、APIキーなしで `npm ci` と本番ビルドが成功すること。`npm audit --omit=dev` の脆弱性0を維持する |

外部APIはテストでモックまたはFake Providerに固定し、**モックの成功を実接続の成功として記録しない**。

---

## 13. デプロイ・環境変数

### 13.1 環境変数

| 分類 | 変数 |
|---|---|
| アプリ共通 | `NEXT_PUBLIC_APP_URL`, `NEXT_PUBLIC_DEMO_MODE_ENABLED`, `NEXT_PUBLIC_DEFAULT_TIME_ZONE` |
| Supabase | `NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY`, `SUPABASE_SECRET_KEY`（サーバー専用） |
| Google Calendar | `GOOGLE_CALENDAR_CLIENT_ID`, `GOOGLE_CALENDAR_CLIENT_SECRET`, `GOOGLE_CALENDAR_REDIRECT_URI`, `GOOGLE_CALENDAR_TOKEN_ENCRYPTION_KEY`, `GOOGLE_CALENDAR_PROVIDER`, `NEXT_PUBLIC_GOOGLE_CALENDAR_DEMO_PROVIDER` |
| Push | `NEXT_PUBLIC_VAPID_PUBLIC_KEY`, `VAPID_PRIVATE_KEY`, `VAPID_SUBJECT`, `PUSH_PROVIDER` |
| AI | `AI_COST_MODE`, `AI_DEFAULT_PROVIDER`, `AI_FALLBACK_PROVIDERS`, `AI_ALLOW_PAID_PROVIDERS`, `GEMINI_API_KEY`, `GEMINI_MODEL`, `GEMINI_FREE_TIER_ONLY`, `GEMINI_*_CAP`, `OLLAMA_BASE_URL`, `OLLAMA_MODEL`, `OLLAMA_ENABLED`, `AI_MAX_CONTEXT_MESSAGES`, `AI_MAX_TOOL_CALLS`, `AI_REQUEST_TIMEOUT_MS`, `AI_EXTERNAL_DATA_CONSENT_REQUIRED` |
| 画像解析 | `FOOD_VISION_ENABLED`, `FOOD_VISION_DEFAULT_PROVIDER`, `FOOD_VISION_FALLBACK_PROVIDERS`, `FOOD_VISION_MAX_FILE_BYTES`, `FOOD_VISION_MAX_PIXELS`, `FOOD_VISION_MAX_DIMENSION`, `FOOD_VISION_DAILY_ANALYSIS_CAP`, `FOOD_VISION_REQUEST_TIMEOUT_MS`, `FOOD_VISION_MAX_ITEMS`, `FOOD_IMAGE_STORAGE_MODE`, `FOOD_IMAGE_RETENTION_HOURS`, `FOOD_IMAGE_BUCKET`, `GEMINI_VISION_MODEL`, `OLLAMA_VISION_MODEL`, `FOOD_VISION_TEST_PROVIDER` |
| ジョブ | `CRON_SECRET` |

- 実値は `.env.example` 以外の `.env*` に置き、Gitへコミットしない。
- 本番環境は `fake` プロバイダー、プレースホルダー値、テスト専用プロバイダーを拒否する。
- 秘密値の検証は値を出力せず、欠落・不正の事実のみを報告する。

### 13.2 配置

- Next.jsをVercel、PostgreSQL/Auth/StorageをSupabaseへ配置する。
- 通知配信はデプロイ側スケジューラーが `POST /api/notifications/dispatch` を `CRON_SECRET` 付きで呼び出す。
- 配置前に HTTPS、環境変数、OAuthコールバックURL、Cron、VAPID、使用量上限、DB migration適用を確認する。
- ステージングは検証専用で、リポジトリのコマンドがデプロイ・外部リソース作成・実資格情報の読み取りを行うことはない。

---

## 14. 初期スコープ外・後続フェーズ検討事項

version2では旧実装の「設計のみで未実装」（ダッシュボード、習慣、ICS出力、AI変更提案フロー）を初期スコープに含めた（5.14〜5.17節）。一方、以下は初期スコープ外とし、必要に応じて `Health_Web_App_マルチエージェント開発要件定義書.md` の手順で追加検討する。

- `report_templates` / `report_exports` の永続化方式の詳細（一覧・再出力UIを持つか、生成のみに留めるか）は実装フェーズで確定する。
- HealthKit / Apple Health / Health Connect 等ネイティブ連携。
- App Store向けネイティブアプリ化。
- Push通知を利用する具体的な項目の最終確定（8種カテゴリの粒度調整）。
- Google Calendar双方向同期の範囲拡張。
- 食品画像認識結果の精度評価方法の確立。

---

## 15. 開発・リリース運用ルール

- ブランチ運用: `main` を保護し、作業は `feature/**`、`fix/**`、`chore/**`、`test/**`、`docs/**`、`release/**` の専用ブランチで行う。`develop` を統合先とし、`main` への統合はPull Requestと承認を経る。
- コミットメッセージは `<フェーズ名>: 変更内容` の形式（`Health_Web_App_マルチエージェント開発要件定義書.md` 9章のテンプレートに従い、担当ツール名を付記する）。
- 秘密情報は `.env` 系ファイルに置き、リポジトリへ含めない。サービスロール鍵・秘密鍵は共有しない。
- リリース前に `npm run env:check` / `release:check` / typecheck / lint / test / build / E2E をすべて成功させる。
