# Health Web App マルチエージェント開発要件定義書
## （Kimi K2.7 Code / Codex / Claude Code 体制版）

作成日: 2026-08-19

---

## 0. 本書の位置づけ

これまで本プロジェクトは **Codex 単独**で実装されており、`要件定義書.md`（実装解析版、以下「実装仕様書」）にその確定仕様が、`Health_Web_App_要件定義書.md`（初期目標版、以下「目標仕様書」）に当初の目標がまとめられている。今後は不具合是正と未実装項目の解消を、**Kimi K2.7 Code / Codex / Claude Code の3ツール体制**で進める。

本書はその体制のための追加要件定義であり、以下を単一の正とする優先順位で運用する。

1. **本書**（体制・分担・品質ゲートのルール）
2. **実装仕様書**（`要件定義書.md`）＝機能・データモデル・APIの確定仕様
3. **目標仕様書**（`Health_Web_App_要件定義書.md`）＝実装仕様書の第14章「未実装・未検証」項目を埋める際の目標値・バックログ

既存2文書の機能要件・データモデル・API仕様・非機能要件を**再定義するものではない**。本書は「誰が・どの順で・どう品質を担保して直すか」だけを扱う。

---

## 1. 背景と目的

- Codex単独実装で複数の不具合が発生した。原因の切り分けをせずに同一ツールで直し続けると、同じ思考パターンに起因する見落としを再発させるリスクがある。
- 3ツールを役割分担させることで、(a) 実装者と別ツールによるレビューでバグ検出率を上げる、(b) 大量の反復的な画面/CRUD実装をコスト効率よく進める、(c) セキュリティ・整合性が絡む中核領域は最も慎重なツールに寄せる、という3点を狙う。
- 目的は「速く直す」ではなく「実装仕様書の内容を、ツールを変えても揺らがない一貫した仕様として再現し続けられる体制を作る」こと。

---

## 2. 各ツールの強み（本プロジェクトでの役割根拠）

ツールの能力は更新が速いため、以下は2026年8月時点の一般的傾向であり、実際の担当決定は3章の判断基準を優先する。

### 2.1 Claude Code
- 複数ファイルにまたがる整合性の維持、RLS・所有権・状態遷移など「壊れると気づきにくい」種類の不具合に対する慎重な検証に強みがある。
- 本プロジェクトはRLS・`row_version`楽観ロック・冪等キー・所有者境界・Google OAuth・オフライン同期キューなど、**一箇所の誤りが他ユーザーのデータ漏洩や二重処理に直結する**領域が多い。これらはClaude Codeを一次実装者、または他ツール成果物の最終レビュアーとする。
- 既存コードの意図を壊さずに直す「外科的な修正」、及びPRの最終統合判断に向く。

### 2.2 Codex
- 本プロジェクトの初期実装者であり、既存コードベースの文脈・実装意図の把握コストが最も低い。
- 一般的なCRUD API・Route Handler・Zodスキーマなど「既存パターンの横展開」で高い生産性を発揮する。
- 既知不具合の原因箇所を最もよく知っている可能性が高いため、**Codexが実装した機能の一次不具合修正の第一候補**とするが、**その修正の妥当性確認は必ず別ツールが行う**（Codex単独実装の再発防止のため）。

### 2.3 Kimi K2.7 Code
- オープンウェイト・長文コンテキスト（256K）・低コスト・エージェント型の長時間タスクに強みがあり、UIスクリーンショットや操作録画などのマルチモーダル入力を扱える。
- 本プロジェクトのように**構造が似た画面・機能が多数存在する**（種目/ルーティン/予定/在庫ロット/買い物リストなど、CRUD＋一覧＋CSV出力のパターンが繰り返される）場合、テンプレート化した実装の量産、テストコードの大量生成、E2Eシナリオの拡充に向く。
- バグ報告に添付されたスクリーンショットや画面録画から不具合を再現するタスク（5.4節参照）にも活用する。

---

## 3. 機能領域ごとの担当割り当て

実装仕様書 第5章の機能区分に沿って、一次実装（Primary）と必須レビュー（Review）を割り当てる。**Reviewは必ずPrimaryと異なるツールが行う。** ここでの割り当ては初期値であり、4章の判断基準に基づき都度見直してよい。

| 実装仕様書の節 | 機能領域 | Primary | 必須Review | 理由 |
|---|---|---|---|---|
| 5.1 | 認証・アカウント（削除/出力含む） | Claude Code | Codex | 再認証・利用者状態遷移・削除順序の誤りは致命的 |
| 5.2 | プロフィール／初回セットアップ | Kimi K2.7 Code | Codex | フォーム主体でパターン化しやすい |
| 5.3 | 身体測定 | Kimi K2.7 Code | Claude Code | CRUD＋グラフ＋CSVの定型画面（※Phase3ではリファレンス実装のためバックエンドはClaude Codeが担当。下記注記参照） |
| 5.4 | 運動・ストレッチ（種目/ルーティン/予定/セッション） | Codex | Claude Code | 状態遷移が複雑（セッション状態機械） |
| 5.5 | 睡眠・水分・体調 | Kimi K2.7 Code | Codex | 定型記録画面が多い |
| 5.6 | サプリメント（在庫FEFO・原子的RPC） | Claude Code | Codex | 負在庫防止・原子的RPCは整合性リスクが高い |
| 5.7 | 食事・レシピ・献立 | Codex | Claude Code | 栄養計算の「不完全」表示ロジックが複雑 |
| 5.8 | 食事画像解析（外部送信同意・保持期限） | Claude Code | Codex | 同意管理・保持期限・秘密情報境界の安全性が最優先 |
| 5.9 | 食品在庫・買い物 | Kimi K2.7 Code | Claude Code | 取引種別が多いが構造は定型 |
| 5.10 | カレンダー（アプリ内） | Codex | Kimi K2.7 Code | 繰り返し予定の展開ロジックはCodexが実装済み把握 |
| 5.11 | Google Calendar連携（OAuth・トークン暗号化） | Claude Code | Codex | OAuth・鍵管理・revoke順序はセキュリティクリティカル |
| 5.12 | 通知（Web Push・秘密値分離） | Claude Code | Codex | 秘密値の公開/非公開分離・スケジューラー専用境界 |
| 5.13 | AIアシスタント（zero-cost routing） | Codex | Claude Code | プロバイダー許可リスト境界の再確認が必要 |
| 6章 | データモデル・migration | Claude Code | Codex | RLS・複合外部キー・row_versionの正しさが最優先 |
| 7章 | API共通境界（same-origin/CSRF/64KiB上限等） | Claude Code | 全員 | 全APIに影響する共通土台のため単独責任者を置く |
| 8章 | オフライン同期・Service Worker | Claude Code | Codex | 冪等性・キャッシュ許可リスト・ペイロード境界 |
| 11章 | UI・アクセシビリティ | Kimi K2.7 Code | Claude Code | 反復的なUI適用作業が中心 |
| 12章 | テスト・CI | Kimi K2.7 Code（生成量産）／Claude Code（契約・RLSテスト） | 相互 | 種類によって適任者が異なる |
| 14.1 | 未実装項目（ダッシュボード等、4章参照） | 都度決定 | 都度決定 | 新規実装のため4章の基準で判断 |

---

### 3.1 Phase3（身体測定・リファレンス実装）の分業特例

身体測定（5.3節）は、後続フェーズでKimi K2.7 Codeが横展開する際の「お手本パターン」を作る位置づけ（本書ロードマップのPhase3）のため、通常のPrimary/Reviewとは別に以下の特例で進める。

- **バックエンド（DB・API）**: Claude Codeが担当する。整合性・RLS・冪等性のパターンを最初に固めるため。
- **フロントエンド（画面・UI）**: Kimi K2.7 Codeが担当する。Phase4以降の定型CRUD画面はここで作られたフロントパターンを踏襲する。
- 進行順序: バックエンド（API契約の確定）→ フロントエンド、の順で行う。フロントエンド着手時は、確定したAPIのリクエスト/レスポンス仕様を前提にする。
- Phase4以降の各機能（5.5, 5.9等）では、この特例は適用せず3章冒頭の表（Primary/Review）どおりに進める。

## 4. 担当判断の基準（表にない/迷う場合）

新規タスクや表に明記のない修正は、以下の質問に沿って割り当てる。

1. **「この処理を誤ると、所有権漏洩・二重処理・秘密情報の露出につながるか？」**
   → Yesなら Primary=Claude Code、Review=Codex または Kimi K2.7 Code。
2. **「似た構造の画面/APIが3つ以上、他にも存在するか？」**
   → Yesなら Primary=Kimi K2.7 Code（量産・コスト効率を優先）。
3. **「Codexによる既存実装のバグ修正で、影響範囲が単一機能に閉じているか？」**
   → Yesなら Primary=Codex（文脈理解コストが最小）。ただしReviewは必須。
4. どれにも当てはまらない場合は Claude Code を Primary とする（安全側に倒す）。

---

## 5. マルチツール運用ルール

### 5.1 単一の仕様源

- 3ツールとも、機能・データモデル・APIの内容は**実装仕様書（`要件定義書.md`）を唯一の正**として参照する。目標仕様書は未実装項目のバックログとしてのみ参照する。
- 実装仕様書と食い違う実装を発見した場合、**その場で仕様を書き換えず**、Issueとして記録し、Claude Codeが最終的に実装仕様書へ反映する（仕様書の更新権限はClaude Codeに一元化し、3者が別々に仕様書を書き換えて食い違いが生じることを防ぐ）。

### 5.2 ブランチ・並行作業

- 既存のブランチ命名規則（`feature/**` `fix/**` `chore/**` `test/**` `docs/**` `release/**`、`main`保護、`develop`統合）を維持する。
- ツールごとの並行作業による衝突を避けるため、ブランチ名に担当ツールを含める（例: `fix/claude-supplement-fefo-negative-stock`、`feature/kimi-pantry-csv-export`）。
- **同一の実装仕様書の節（5.x）を複数ツールが同時に別ブランチで編集しない。** 表の担当割り当てが競合する場合は着手前に調整する。
- コミットメッセージ規約は実装仕様書 15章の形式（`<フェーズ名>: 変更内容`）を維持し、末尾に担当ツール名を付記する（例: `fix: prevent negative stock on void intake (claude-code)`）。

### 5.2a Git操作の自動化（コミット・プッシュ）

各エージェント（Claude Code / Codex / Kimi K2.7 Code）は、以下の条件をすべて満たした時点で、**ユーザーへの都度の確認を待たずに自動的に`git add` → `git commit` → `git push`まで実行する。**

1. 担当タスク（フェーズ単位、または一つの機能単位）の実装が完了している。
2. 7章の品質ゲート（`typecheck` / `lint` / `format` / `test` / `build`。DB変更を伴う場合は追加でmigration検証）が**すべてローカルで通過**している。
3. 作業対象が自分の担当ブランチ（5.2節の命名規則に従うfeature/fixブランチ）であり、`main`および`develop`への直接コミットではない。

上記を満たさない場合（品質ゲート未通過、担当外の節への変更、仕様の解釈が割れている等）は、**コミット・プッシュせずに作業を止めてユーザーに報告する。**

- push後、エージェントは「ブランチ名・変更概要・実装仕様書の該当節・品質ゲートの結果」を要約してユーザーに報告する。
- `main`・`develop`への統合（マージ）は自動化の対象外とし、5.3節のレビュー条件を満たした上で人間またはClaude Codeが明示的に行う。**自動化されるのは各ツールの担当ブランチへのpushまでであり、統合ブランチへの反映は別工程とする。**
- pushする際、コミット・PR説明文に秘密情報（APIキー、トークン等）が含まれていないことを、8章のリスク対策に基づき機械的にも確認してからpushする（`.env`系ファイルが`git status`に含まれていないことの確認を含む）。
- 各エージェントへの指示プロンプトには、上記の自動push条件を明記すること（10章のプロンプトテンプレートを参照）。

### 5.3 レビュー・マージ条件

- PRは Primary 以外の少なくとも1ツール（表のReview欄）による差分確認を経てからマージする。
- 以下に触れるPRは、担当に関わらず**必ずClaude Codeの最終承認**を要する。
  - RLSポリシー・migration・複合外部キー
  - 認証・再認証・利用者状態（`users.status`）
  - Google OAuthトークンの暗号化・保存・revoke
  - Push購読の秘密値分離
  - 削除系RPC（健康データ削除・アカウント削除）
  - 冪等性・`row_version`楽観ロックに関わる変更
- 上記以外の一般的なCRUD/UI変更は、表のPrimary/Reviewの2ツール合意でマージ可とする。

### 5.4 バグ修正の進め方（既知不具合の解消）

1. 不具合ごとに「発生画面／再現手順／期待動作（実装仕様書の該当節番号）／実際の動作」をIssue化する。UIバグはKimi K2.7 Codeがスクリーンショットまたは操作録画から再現手順の言語化を担当できる。
2. 3章の表に沿ってPrimaryが原因箇所を特定し修正する。**実装仕様書の記述を変えずに直せる不具合か、仕様自体の解釈が割れている不具合かを最初に切り分ける**（後者は5.1節の手順でClaude Codeが仕様側を確定させてから着手する）。
3. Reviewツールは、修正内容が実装仕様書の該当節（値域、状態遷移、権限境界など）を満たすかを実装仕様書と突き合わせて確認する。
4. 12章の既存テスト（typecheck / lint / test / build / E2E）が通ることを、担当ツールに関わらず共通のマージ条件とする。ツールを変えても品質ゲートは変えない。

### 5.5 秘密情報・環境変数

- 3ツールとも実装仕様書 9.2節・13.1節の秘密情報境界を厳守する。サービスロール鍵・OAuthクライアントシークレット・VAPID秘密鍵・AI APIキーをコード・ログ・コミットへ含めない。
- 各ツールがローカル/CIで使用する環境変数は、実装仕様書 13.1節の一覧を単一の正とする。ツール独自の追加環境変数を無断で導入しない（必要な場合は本書5.1節の手順でIssue化してから追加する）。
- `npm run env:check` および `release:check` は、どのツールが変更した場合でもマージ前に必須とする。

---

## 6. 未実装・設計のみの項目の扱い（実装仕様書14.1章の解消）

実装仕様書14.1章に列挙された未実装項目は、目標仕様書を参照しつつ以下の担当で新規実装する。着手前に必ず5.1節の手順で「実装仕様書への追記案」をClaude Codeがレビューし、範囲を確定してから着手する。

| 項目 | 目標仕様書の該当箇所 | 想定Primary | 備考 |
|---|---|---|---|
| ダッシュボード（`/`の今日の状況・クイック操作） | 5.2 ホーム | Kimi K2.7 Code | 表示集約が中心で定型的 |
| 習慣（habits）専用画面・API | — | Codex | 既存の`habits`/`habit_logs`テーブル・出力経路をCodexが把握 |
| カレンダーICS出力 | — | Kimi K2.7 Code | CSV出力の横展開として実装可能 |
| AI変更提案の承認フローUI | 5.11 AIアシスタント要件（診断として扱わない等） | Claude Code | `ai_change_proposals`の書込境界（server-only）を伴うため |
| `report_templates`/`report_exports`の永続化 | 5.10 レポートページ | 都度決定 | 永続化要否を先に意思決定（本書5.1節の手順）してから着手 |

新規実装であっても、5.3節「必須Claude Code承認」の対象（RLS・権限境界に触れる場合）は同様に適用する。

---

## 7. 品質ゲート（共通・ツール非依存）

実装仕様書12章の内容をマルチツール体制下でも**変更せず共通の合格基準**とする。

- 静的検査: `npm run typecheck` / `npm run lint` / `npm run format`
- 単体・結合: `npm test`（Vitest）
- DB: PGliteによるmigration検証、スキーマ契約テスト。Docker利用時は `supabase db reset` / `db lint` / `supabase test db`（pgTAP）
- E2E: Playwright（Chromium / Mobile Safari）
- 環境検証: `npm run env:check`、`release:check`
- 依存管理: `npm audit --omit=dev` 脆弱性0を維持

**どのツールが書いたコードでも、上記が全て通らなければマージしない。** 外部APIはモック/Fake Providerで固定し、モック成功を実接続成功として記録しない（実装仕様書12章の原則を継続）。

---

## 8. リスクと留意事項

- **ツール間の実装スタイルの差異**: 同じ要件でもツールごとにコーディングスタイル・エラーハンドリングの流儀が異なりうる。既存のESLint/Prettier設定と`src/tests/server-only.ts`のようなアーキテクチャ制約は、ツールに関わらず必ず踏襲させる。
- **仕様の分散管理リスク**: 3ツールがそれぞれ独自解釈で進めると仕様が発散する。5.1節の「仕様源の一元管理」を徹底し、実装仕様書の更新はClaude Codeのみが行う運用を崩さない。
- **秘密情報の混入リスク**: ツール数が増えるほど、ログ・コミット・PR説明文への秘密値混入経路が増える。PRテンプレートに「秘密情報を含んでいないことの確認」チェック項目を追加することを推奨する。
- **重複実装・競合**: 5.2節のブランチ運用を守らないと、同一領域を2ツールが並行して書き換え、マージコンフリクトや仕様の逆行が起きる。着手前の担当表確認を必須の手順とする。
- **ベンダー選定の再評価**: 各ツールの実際の性能・コストは頻繁に更新される。3章の割り当ては固定ルールではなく、明らかな性能ギャップが判明した場合は本書自体を改訂して見直す。

---

## 9. 開発ロードマップとフェーズ進捗

ゼロからの再構築は、土台（DBスキーマ・認証・共通境界）を誤ると全機能に波及するため、**最初はClaude Code単独で土台を固め、パターンが固まってから3ツールに分業を広げる**順序で進める。

### 9.1 フェーズ一覧

| フェーズ | 内容 | 担当 | 理由 |
|---|---|---|---|
| Phase 0 | プロジェクト初期化・共通基盤（Next.js/TS/Tailwind/lint/test設定、ディレクトリ構成、env雛形） | Claude Code | 全員がこの上に乗るので最初は単独で固める |
| Phase 1 | DBスキーマ基盤（users/auth trigger、RLS基本方針、row_version・冪等キー・複合外部キーのパターン） | Claude Code | 実装仕様書6章。誤ると全機能に波及する最重要領域 |
| Phase 2 | 認証・アカウント基盤（5.1節：ログイン/セッション/保護ルート/利用者状態） | Claude Code | セキュリティクリティカル |
| Phase 3 | リファレンス実装＝身体測定（5.3節）をフルスタックで1本通す | バックエンド: Claude Code<br>フロントエンド: Kimi K2.7 Code | 以降の全CRUD機能の「お手本」になる実装パターンを作る（3.1節の分業特例を適用） |
| Phase 4〜 | 定型CRUD群（睡眠/水分/体調、サプリ商品・在庫、食品/レシピ、パントリー） | バックエンド（DB migration・API）: Claude Code<br>フロントエンド: Kimi K2.7 Code<br>レビュー: Codex | Phase3の分業（3.1節）を恒久化。6章「データモデル・migration」はClaude Code担当という3章表の原則と、5.5節等でKimiを単独Primaryとする表記の間で齟齬があったため、Phase3の実績（バックエンド先行→フロント→レビューで409/冪等性等の重大バグを複数検出できた）に合わせて統一する |
| Phase 4〜 | フロー複雑系（運動セッション状態機械、カレンダー展開、食事画像解析、通知配信、AIアシスタント） | Codex | 状態遷移・外部連携が絡む中規模機能 |
| Phase 4〜 | Google OAuth連携、Push秘密値分離、削除フロー、レビュー統括 | Claude Code | セキュリティクリティカル継続 |

Phase 4以降、各機能の一次担当・レビュー担当は3章の表を参照しつつ、DB migrationとAPIバックエンドは常にClaude Codeが担当し、フロントエンドは3章表のPrimary（多くはKimi K2.7 Code）が担当する。レビューは3章表のReview欄に加え、Codexによる最終確認を基本とする。

### 9.2 進捗状況

このフェーズごとに現在の状態を追記・更新していく。各フェーズの完了時、および担当ツールの変更等があった場合はこの表を更新する。

| フェーズ | 状態 | 備考 |
|---|---|---|
| Phase 0 | 完了 | feature/phase0-project-init。develop統合済み（04dc297） |
| Phase 1 | 完了 | feature/phase1-db-foundation。Codexレビュー2往復（Critical 2件→Should-fix含め解消）を経てdevelop統合済み |
| Phase 2 | 完了 | feature/phase2-auth-foundation。Codexレビュー2往復（メール確認フロー不整合、proxy状態未確認、same-origin迂回のCritical 3件）を経てdevelop統合済み（04dc297） |
| Phase 3a（身体測定・バックエンド） | 完了 | feature/phase3a-measurements-backend。Codexレビュー3往復（既定種別偽装防止、冪等性の履歴化、BMI単位是正等）を経てdevelop統合済み（de33f2c） |
| Phase 3b（身体測定・フロントエンド） | 完了 | feature/phase3b-measurements-frontend。Claude Codeレビュー6往復（409競合からの回復不能パターンが複数条件で繰り返し発見・修正）を経てdevelop統合済み（38e10b2） |
| Phase 4-1a（睡眠・水分・体調・バックエンド） | レビュー待ち | feature/phase4-1a-wellness-backend（ed68d16）。Phase3bの教訓（409後の対象特定失敗）を設計段階で回避（所有者＋記録日時＋種別の一意制約）。Codexレビュー待ち |
| Phase 4〜（それ以降） | 未着手 | 9.1節の分業方針（バックエンド: Claude Code／フロントエンド: Kimi K2.7 Code／レビュー: Codex）で進める |

develop HEAD: `38e10b2`（Phase 3b統合時点。Phase 4-1aはレビュー中のため未統合）

---

## 10. エージェント指示プロンプトの共通テンプレート

各フェーズ・各機能の指示プロンプトには、末尾に以下のブロックを必ず含める（5.2a節の自動push運用を各エージェントに徹底させるため）。

```
## Git運用（自動化）
- 作業ブランチ: <ブランチ名。5.2節の命名規則に従うこと。例: fix/claude-xxx, feature/kimi-xxx>
- 実装が完了し、以下がすべてローカルで通過したら、確認を待たずに
  git add / git commit / git push まで自動的に実行してください。
  - npm run typecheck
  - npm run lint
  - npm run format（差分なしを確認、または自動整形後に差分をコミット）
  - npm test
  - npm run build
  - （DBスキーマ変更を伴う場合）migration検証
- 品質ゲートのいずれかが通らない場合、または担当外の実装仕様書の節に
  変更が及ぶ場合は、コミット・プッシュせずに作業を止めて報告してください。
- push先は上記の作業ブランチのみとし、main/developへは直接pushしないでください。
- コミットメッセージは「<フェーズ名>: 変更内容 (<担当ツール名>)」の形式にしてください。
- push前に .env 系ファイルや秘密情報が git status に含まれていないことを確認してください。
- 完了したら、変更概要・実装仕様書の該当節・品質ゲートの結果・pushしたブランチ名を
  簡潔に報告してください。
```

---

## 11. 本書の改訂

- 担当表（3章）・判断基準（4章）は、実際の運用結果（レビューで見つかった不具合の傾向、各ツールの得意/不得意の実績）に基づき定期的に見直す。
- 実装仕様書・目標仕様書・本書の整合性は、リリース判定（実装仕様書15章相当のリリース運用ルール）の一項目として都度確認する。
